(_
  (identifier) @func_name
  (parameter_list) @param_list
  return: (_)? @return_type
  (dictionary)? @return_type)

(_
  (ERROR
  (identifier) @func_name
  (parameter_list) @param_list
  return: (_)? @return_type
  (dictionary)? @return_type))